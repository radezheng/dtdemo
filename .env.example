# Copy this file to .env and fill in your real connection strings.
# .env is ignored by git (see .gitignore).

# Used by send_orders.py (preferred) or both scripts if you only set the generic var.
EVENTHUB_SEND_CONNECTION_STRING=

# Used by receive_orders.py (preferred) or both scripts if you only set the generic var.
EVENTHUB_RECEIVE_CONNECTION_STRING=

# Optional generic fallback used by both scripts.
EVENTHUB_CONNECTION_STRING=
